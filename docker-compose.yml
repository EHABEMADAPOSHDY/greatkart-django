services:
  django-app:
    container_name: django_app_container

    build:
      context: .
    
    volumes:
      - .:/app
    
    ports:
      - "8000:8000"

    command: python manage.py runserver 0.0.0.0:8000